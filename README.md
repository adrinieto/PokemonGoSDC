PokemonGoSDC
============


# Instalación

1. Instala las dependencias con `pip install -r requirements.txt`
2. Añade los datos de la cuenta para escanear y la API key para el Bot de Telegram en `config.py`
3. Ejecutar con `python gyms_scanner.py`